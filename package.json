{
  "name": "startup-graveyard",
  "version": "1.3.0",
  "description": "Structured startup failure research and evidence-gated publishing workflows.",
  "license": "MIT",
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "eslint . && turbo run lint",
    "lint:fix": "eslint . --fix",
    "test": "turbo run test",
    "test:pg": "pnpm --filter @sg/api test:pg",
    "test:e2e": "node scripts/run_browser_e2e.mjs",
    "validate:migrations": "node scripts/validate_migrations.mjs",
    "typecheck": "turbo run typecheck",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "containers:build": "docker build -f ops/docker/Dockerfile.migrate -t startup-graveyard-migrate:local . && docker build -f ops/docker/Dockerfile.seed -t startup-graveyard-seed:local . && docker build -f ops/docker/Dockerfile.api -t startup-graveyard-api:local . && docker build -f ops/docker/Dockerfile.web -t startup-graveyard-web:local .",
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{ts,tsx,mts,cts}": [
      "eslint --fix --no-warn-ignored --max-warnings 0",
      "prettier --write"
    ],
    "*.{js,mjs,cjs}": [
      "prettier --write"
    ],
    "*.{json,yaml,yml,md}": [
      "prettier --write"
    ]
  },
  "devDependencies": {
    "@commitlint/cli": "^20.5.0",
    "@commitlint/config-conventional": "^20.5.0",
    "@eslint/js": "^10.0.1",
    "@playwright/test": "^1.62.1",
    "@types/pg": "^8.20.0",
    "eslint": "^10.2.0",
    "eslint-config-next": "^16.2.3",
    "eslint-plugin-react": "^7.37.5",
    "eslint-plugin-react-hooks": "^7.0.1",
    "globals": "^17.4.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "pg": "^8.14.1",
    "prettier": "^3.4.2",
    "turbo": "^2.5.0",
    "typescript": "^5.8.0",
    "typescript-eslint": "^8.58.1"
  }
}
