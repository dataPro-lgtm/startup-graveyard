# 生产环境配置示例
# 复制为 .env 并修改相应值

# 环境
NODE_ENV=production

# 端口（默认3000）
PORT=3000

# API Keys (可选)
OPENAI_API_KEY=
DEEPSEEK_API_KEY=

# 采集API Token (可选，用于保护采集接口)
COLLECT_API_TOKEN=

# 定时任务配置（cron表达式）
CRON_SCHEDULE=0 2 * * *

# Next.js配置
NEXT_PUBLIC_APP_URL=http://your-domain.com
