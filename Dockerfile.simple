# 简化版 Dockerfile（不使用多阶段构建）
FROM node:18-alpine

WORKDIR /app

# 复制package文件
COPY package.json package-lock.json* ./

# 安装依赖
RUN npm ci --only=production

# 复制所有文件
COPY . .

# 初始化数据
RUN npm run init-data || node scripts/init-data.mjs || true

# 构建应用
RUN npm run build

# 暴露端口
EXPOSE 3000

# 设置环境变量
ENV NODE_ENV=production
ENV PORT=3000
ENV HOSTNAME="0.0.0.0"

# 启动应用
CMD ["npm", "start"]
